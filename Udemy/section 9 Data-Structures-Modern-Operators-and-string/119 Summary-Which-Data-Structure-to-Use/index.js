console.log(`
1). from the program itsele : status message,
2). From the Ui : from user,
3). from External Sources : web API

data structure
A. Simple List // simple list
Array or Sets
B. Objects or Maps // key pairs



`);
console.log(`convert object To Array`);
const conObjToArr = Object.entries(openingHours);
console.log(conObjToArr);
